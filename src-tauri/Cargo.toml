[package]
name = "timetips"
version = "0.1.0"
description = "TimeTips — Desktop exam practice timer"
authors = ["TimeTips Contributors"]
edition = "2021"

[lib]
name = "timetips_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.0", features = [] }
tauri-plugin-shell = "2.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
